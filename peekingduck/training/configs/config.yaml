device: "auto"
project_name: "cifar10" #rsna | cifar10
debug: True
framework: "pytorch" # 'pytorch' | 'tensorflow'

defaults:
  - use_case: common
  - data_module: cifar10 #rsna | cifar10
  - model: ${use_case}
  - trainer: ${use_case}
  - callbacks: ${use_case}
  - metrics: ${use_case}
  - stores: ${use_case}
  - _self_
  - override hydra/job_logging: custom

hydra:
  run:
    dir: "outputs/${project_name}/${stores.unique_id}"
