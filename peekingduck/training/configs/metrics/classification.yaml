pytorch:
  evaluate:
    ["accuracy", "precision", "recall", "auroc", "multiclass_calibration_error"]
  adapter:
    _target_: src.metrics.pytorch_metrics.torchMetrics

tensorflow:
  evaluate:
    ["accuracy", "precision", "recall", "auroc"]
  adapter:
    _target_: src.metrics.tensorflow_metrics.tensorflowMetrics
